let type ?= "app"
let type ?= "lib"

let command = "require \"../test_lib\""

output ${type} out/${_platform}_${_arch}_${_config}/$app(test)

define LOL "\"haha\""

std c++17

incpath ../test_lib

# require ../test_lib

$[command]

lib ../test_lib/lib/${_platform}_${_arch}_${_config}/$lib(test)

# if ${_platform} = win32 msg "Hello Windows"
# else msg "Hello Linux"
# endif